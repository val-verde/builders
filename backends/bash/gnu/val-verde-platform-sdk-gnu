#!/bin/bash

set -e

# Source package build helper functions
source /usr/libexec/${VAL_VERDE_GH_TEAM}-platform-sdk-builder-api
source /usr/libexec/${VAL_VERDE_GH_TEAM}-platform-sdk-builder-utils

# llvm dependencies
ECHO=/usr/bin/echo \
package-invoke-category-builder llvm-dependencies-gnu

# llvm build
ENABLE_STAGE_ROOT_BUILD=TRUE \
package-invoke-builder llvm-project 13

# souper build
package-invoke-builder souper-cross 1

# cmark build
package-invoke-builder swift-cmark-cross 5 none

# swift build
package-invoke-builder swift 5

# lldb build
ENABLE_STAGE_ROOT_BUILD=TRUE \
package-invoke-builder swift-lldb 5

# swift sdk build
category-invoke-builder swift-sdk-cross

# go build
package-invoke-builder go-cross 1

# ruby build
package-invoke-builder ruby-cross 3

# graphics sdk build
category-invoke-builder graphics-sdk-cross

# virtualization sdk build
category-invoke-builder virtualization-sdk-cross

# node + sdk build
package-invoke-builder node-cross 16
package-invoke-builder npm-yarn-cross 1

# yautja server build
package-invoke-builder yarn-yautja-server-cross 1

# wine build
package-invoke-builder wine-cross 6

# blockchain sdk build
category-invoke-builder blockchain-sdk-cross
