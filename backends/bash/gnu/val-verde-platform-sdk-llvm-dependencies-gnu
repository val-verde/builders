#!/bin/bash

set -e

# zlib build
CMAKE_BINDIR=/usr/bin \
NINJA=/usr/bin/ninja \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-zlib-cross

# bzip2 build
MAKE=/usr/bin/make \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-bzip2-cross

# lz4 build
MAKE=/usr/bin/make \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-lz4-cross

# xxhash build
MAKE=/usr/bin/make \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-xxhash-cross

# zip build
MAKE=/usr/bin/make \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-zip-cross

# zstd build
CMAKE_BINDIR=/usr/bin \
NINJA=/usr/bin/ninja \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-zstd-cross

# unzip build
MAKE=/usr/bin/make \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-unzip-cross

# autoconf build
MAKE=/usr/bin/make \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-autoconf-cross

# automake build
MAKE=/usr/bin/make \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-automake-cross

# libtool build
MAKE=/usr/bin/make \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-libtool-cross

# m4 build
MAKE=/usr/bin/make \
MAKEINFO=/usr/bin/true \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-m4-cross

# make build
MAKE=/usr/bin/make \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-make-cross

# ninja build
CMAKE_BINDIR=/usr/bin \
NINJA=/usr/bin/ninja \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-ninja-cross

# libiconv build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-libiconv-cross

# glib build
MESON=/usr/bin/meson \
PKG_CONFIG=/usr/bin/pkg-config \
PYTHON=/usr/bin/python3 \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-glib-cross

# pkg-config build
PKG_CONFIG=/usr/bin/pkg-config \
PYTHON=/usr/bin/python3 \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-pkg-config-cross

# pcre build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-pcre-cross

# icu build
PYTHON=/usr/bin/python3 \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-icu4c

# xz build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-xz-cross

# libxml2 build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-libxml2-cross

# libffi build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-libffi-cross

# gdbm build
CFLAGS="\
    -fcommon \
    ${CFLAGS} \
" \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-gdbm-cross

# ncurses build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-ncurses-cross

# libedit build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-libedit-cross

# sqlite3 build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-sqlite-cross

# openssl build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-openssl-cross

# libcap build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-libcap-cross

# libexpat build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-expat-cross

# xorg-macros build
PYTHON=/usr/bin/python3 \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-xorg-macros-cross

# xorgproto build
PYTHON=/usr/bin/python3 \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-xorg-xorgproto-cross

# xcb-proto build
PYTHON=/usr/bin/python3 \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-xcb-proto-cross

# xcb-pthread-stubs build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-xcb-pthread-stubs-cross

# libxau build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-xorg-libxau-cross

# libxcb build
PYTHON=/usr/bin/python3 \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-xcb-libxcb-cross

# pixman build
MESON=/usr/bin/meson \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-pixman-cross

# xorg-libpciaccess build
MESON=/usr/bin/meson \
PYTHON=/usr/bin/python3 \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-xorg-libpciaccess-cross

# xorg-libxtrans build
PYTHON=/usr/bin/python3 \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-xorg-libxtrans-cross

# libx11 build
PYTHON=/usr/bin/python3 \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-libx11-cross

# libxext build
PYTHON=/usr/bin/python3 \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-libxext-cross

# xorg-libxrender build
PYTHON=/usr/bin/python3 \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-xorg-libxrender-cross

# xorg-libxfixes build
PYTHON=/usr/bin/python3 \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-xorg-libxfixes-cross

# xorg-libxi build
PYTHON=/usr/bin/python3 \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-xorg-libxi-cross

# xorg-libice build
PYTHON=/usr/bin/python3 \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-xorg-libice-cross

# xorg-libsm build
PYTHON=/usr/bin/python3 \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-xorg-libsm-cross

# xorg-libxt build
PYTHON=/usr/bin/python3 \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-xorg-libxt-cross

# xorg-libxtst build
PYTHON=/usr/bin/python3 \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-xorg-libxtst-cross

# xorg-libxrandr build
PYTHON=/usr/bin/python3 \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-xorg-libxrandr-cross

# xorg-xinput build
# PYTHON=/usr/bin/python3 \
# bash ${VAL_VERDE_GH_TEAM}-platform-sdk-xorg-xinput-cross

# freetype2 build
CMAKE_BINDIR=/usr/bin \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-freetype2-cross

# wayland build
PYTHON=/usr/bin/python3 \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-wayland

# wayland-protocols build
PYTHON=/usr/bin/python3 \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-wayland-protocols-cross

# mesa-drm build
MESON=/usr/bin/meson \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-mesa-drm-cross

# alsa-lib build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-alsa-lib-cross

# cups build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-cups-cross

# libuv build
CMAKE_BINDIR=/usr/bin \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-libuv-cross

# libssh2 build
CMAKE_BINDIR=/usr/bin \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-libssh2-cross

# ldns build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-ldns-cross

# openssh-portable build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-openssh-portable-cross

# curl build
CMAKE_BINDIR=/usr/bin \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-curl-cross

# gettext build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-gettext-cross

# attr build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-attr-cross

# acl build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-acl-cross

# bash build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-bash-cross

# util-linux build
PYTHON=/usr/bin/python3 \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-util-linux-cross

# libarchive build
CMAKE_BINDIR=/usr/bin \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-libarchive-cross

# libmicrohttpd build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-libmicrohttpd-cross

# elfutils build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-elfutils-cross

# grep build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-grep-cross

# sed build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-sed-cross

# bison build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-bison-cross

# gawk build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-gawk-cross

# strace build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-strace-cross

# gperf build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-gperf-cross

# fontconfig build
MESON=/usr/bin/meson \
PYTHON=/usr/bin/python3 \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-fontconfig-cross

# tar build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-tar-cross

# coreutils build
MAKEINFO=/usr/bin/true \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-coreutils-cross

# findutils build
PYTHON=/usr/bin/python3 \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-findutils-cross

# dpkg build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-dpkg-cross

# rsync build
PYTHON=/usr/bin/python3 \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-rsync-cross

# systemd build
MESON=/usr/bin/meson \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-systemd

# wget build
PYTHON=/usr/bin/python3 \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-wget-cross

# python build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-python-cross

# mako build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-mako-cross

# meson build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-meson-cross

# gobject-introspection build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-gobject-introspection-cross

# libsecret build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-libsecret-cross

# tensorflow build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-tensorflow-cross

# lua build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-lua-cross

# swig build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-swig-cross

# java build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-openjdk-cross

# z3 build
CMAKE_BINDIR=/usr/bin \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-z3-cross

# git build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-git-cross

# vim build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-vim-cross

# cmake build
CMAKE_BINDIR=/usr/bin \
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-cmake-cross

# android patch elf
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-android-patch-elf-cross

# jwasm (ml64) cross compiler build
bash ${VAL_VERDE_GH_TEAM}-platform-sdk-jwasm
