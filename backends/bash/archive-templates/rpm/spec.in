Name:       PACKAGE_NAME
Version:    VERSION
Release:    1
Summary:    PACKAGE_NAME source rpm
License:    Apache
BuildArch:  BUILD_PROCESSOR
Source: ARCHIVE_ROOT/SOURCES/PACKAGE_NAME.tar.gz

%description
PACKAGE_NAME source rpm for SYSTEM_ARCH

%prep
%setup -c -n PACKAGE_NAME-%{version}

%build

%install
mkdir -p ARCHIVE_ROOT/BUILDROOT/PACKAGE_NAME-%{version}-RELEASE.BUILD_PROCESSOR
cp -r ARCHIVE_ROOT/PACKAGE_NAME/BUILD/PACKAGE_NAME-%{version}/* ARCHIVE_ROOT/PACKAGE_NAME/BUILDROOT/PACKAGE_NAME-VERSION-RELEASE.BUILD_PROCESSOR/

%files

%clean
rm -rf ARCHIVE_ROOT/BUILDROOT/*

%changelog
